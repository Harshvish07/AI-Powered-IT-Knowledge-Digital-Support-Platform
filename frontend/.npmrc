# On Windows, npm's default script-shell (cmd.exe) mis-parses this project's
# absolute path because the folder name contains an "&" character, which
# cmd.exe treats as a command separator. Running scripts through bash (Git
# Bash) avoids that. See howtocreate.md (Phase 1) for details.
script-shell=C:/Program Files/Git/usr/bin/bash
